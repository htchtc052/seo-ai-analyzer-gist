# Инструмент тестовый и в поиске ему делать нечего.
# Правило на каждую версию отдельной строкой: чтобы открыть одну,
# достаточно убрать её строку, не трогая остальные.
User-agent: *
Disallow: /v1/
Disallow: /v2/
Disallow: /v3/
Disallow: /
