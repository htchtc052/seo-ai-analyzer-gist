# Инструмент тестовый, в поиске ему делать нечего.
# Строка на версию: чтобы открыть одну, убери её строку.
# Общего "Disallow: /" здесь нарочно нет — он перекрывал бы строки версий,
# и открыть отдельную версию было бы нельзя. Сам корень отдаёт noindex,
# а чтобы краулер это увидел, обход корня запрещать нельзя.
User-agent: *
Disallow: /v1/
Disallow: /v2/
Disallow: /v3/
